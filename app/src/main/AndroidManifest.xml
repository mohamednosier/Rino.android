<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rinho.payment">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name="com.rinho.payment.BaseApp"
        android:allowBackup="true"
        android:icon="@drawable/final_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/final_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.statusBarLight"
        android:usesCleartextTraffic="true"
        tools:replace="android:name">
        <activity android:name="com.rinho.payment.NotificationActivity"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api" /> <!-- <activity -->
        <!-- android:name="com.gama.task.ui.splash.SplashActivity" -->
        <!-- android:theme="@style/SplashTheme" -->
        <!-- android:screenOrientation="portrait"> -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".ui.LanguageActivity" -->
        <!-- android:theme="@style/AuthTheme" -->
        <!-- android:screenOrientation="portrait" -->
        <!-- android:windowSoftInputMode="adjustResize" /> -->
        <activity
            android:name="com.rinho.saudi2go.ui.auth.AuthActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AuthTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.rinho.payment.ui.main.MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" /> <!-- <service android:name=".MyFirebaseInstanceIDService"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.google.firebase.INSTANCE_ID_EVENT" /> -->
        <!-- </intent-filter> -->
        <!-- </service> -->
        <service
            android:name="com.rinho.payment.models.MyFirebaseMessagingService"
            android:exported="false"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>